<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools" android:id="@+id/bottom_navigation_graph"
            app:startDestination="@id/homeFragment">

    <fragment android:id="@+id/homeFragment"
              android:name="{{app_id}}.HomeFragment"
              android:label="fragment_home"
              tools:layout="@layout/fragment_home"/>

    <fragment android:id="@+id/layoutGuideFragment"
              android:name="{{app_id}}.LayoutGuideFragment"
              android:label="fragment_layout_guide"
              tools:layout="@layout/fragment_layout_guide"/>

    <fragment android:id="@+id/notificationFragment"
              android:name="{{app_id}}.NotificationFragment"
              android:label="fragment_notification"
              tools:layout="@layout/fragment_notification"/>
</navigation>
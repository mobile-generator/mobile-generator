/**
 * TemplateType
 * Used to distinguish different templates
 */
export enum TemplateType {
    SingleView = 'single-view',
    Drawer = 'drawer',
    Tabbar = 'tabbar',
}

/**
 * PlatformType
 * Used to distinguish different platform
 */
export enum PlatformType {
    Android = 'Android',
    IOS = 'iOS',
    Flutter = 'Flutter',
}
